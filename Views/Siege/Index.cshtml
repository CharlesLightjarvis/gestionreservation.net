@model IEnumerable<GestionReservation.Data.SiegeDisponible>

@{
    ViewData["Title"] = "Index";
}

<h1>Liste Sieges</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table shadow-lg">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodeSiege)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disponibilite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Classe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prix)
            </th>
            <th>
                Numero_vol
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CodeSiege)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Disponibilite)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Classe)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Prix)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdVolNavigation.NumeroVol)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdSiege" class="btn btn-primary">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdSiege" class="btn btn-success">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdSiege" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
